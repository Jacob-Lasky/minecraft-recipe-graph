[
  {
    "modid": "mcrecipedump",
    "name": "Recipe Dump",
    "description": "In-game crafting planner, plus the recipe dumper the offline tools are built on. On a client with JEI it adds /recipedump, which writes every recipe JEI knows about to NDJSON along with the ore dictionary and display names. Loads on servers too, because the planner's item and per-player state have to. Changes no other gameplay.",
    "version": "${version}",
    "mcversion": "${mcversion}",
    "url": "https://github.com/Jacob-Lasky/minecraft-recipe-graph",
    "authorList": ["Jacob Lasky"]
  }
]
