data/
data/graph.json
data/*.ndjson
*.mca
mod/build/
mod/.gradle/
mod/localmaven/
mod/run/
plan*.html
*.pyc
__pycache__/
mod/logs/

# Dev tooling for tools/mobile-audit.js. `pnpm-lock.yaml` is deliberately NOT here:
# committing it is most of the point of pnpm, and it is what makes
# `pnpm install --frozen-lockfile` reproducible.
node_modules/

# Internal working notes. Kept on disk, never published: this repo is public, and these are
# a running conversation with Jake plus a session handoff, not documentation anyone else can
# use. Untracked rather than deleted because they are still the live to-do list.
ASK-JAKE.md
.planning/
dist/

# `tools/ci-java.sh`'s scratch tree: downloaded junit/gson jars and the class files of the
# pure-Java core. Outside `mod/build/` on purpose -- a `mod/build/` written by a container
# running as another uid is the "the tests pass and the build still fails" trap in the skill.
.ci-java/
